import {List, Map} from 'immutable';

export const INITIAL_STATE = Map();

export function setFollowers(state, userEntry) {
	// Skeleton
}

export function setTweets(state, tweetEntry) {
	// Skeleton
}